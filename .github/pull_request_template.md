## What this PR does

<!-- One or two sentences. -->

## Checklist

- [ ] Tests pass: `python3 -m unittest test_cowork_sync -v`
- [ ] `--check` runs clean against real session data (if parsing logic changed)
- [ ] No hardcoded local paths
- [ ] No new pip dependencies (stdlib only)
- [ ] Security tests in `TestSecurity` not regressed
- [ ] CLAUDE.md updated (if repo structure or design decisions changed)
