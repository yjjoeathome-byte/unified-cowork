{
    "_comment": "Linux example — copy to config.json. Tilde (~) expands to your home directory.",

    "_sessions_dir_note": "Leave as \"auto\" (recommended). To pin a literal path: ~/.config/Claude/local-agent-mode-sessions",
    "sessions_dir": "auto",

    "output_dir": "~/Documents/claude-sessions",

    "state_file": "~/.local/share/cowork-sync-state.json",

    "project_tags": {
        "my-project": [
            "keyword1",
            "keyword2",
            "specific-tool-name"
        ]
    },

    "format": {
        "_comment": "Do not edit unless the JSONL *schema* changes. Location/prefix/depth are handled automatically.",
        "session_dir_prefix": "local_",
        "transcript_filename": "audit.jsonl",
        "min_file_size_bytes": 1024,
        "expected_entry_types": ["system", "user", "assistant", "result", "tool_use_summary", "rate_limit_event"],
        "expected_init_fields": ["session_id", "model", "cwd", "mcp_servers"]
    }
}
