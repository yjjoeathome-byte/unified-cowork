# Pending changes from main (as of v1.0.0 — 2026-02-27)

The `main` branch has 2 commits not yet merged into `python-macos`.
These affect documentation and the catch-up protocol — no PowerShell-only
changes, so they are relevant to the Python/macOS path.

## Commits to cherry-pick or merge

1. `912801d` — docs: rewrite catch-up protocol for SESSION-INDEX + archive filtering
   - `examples/catch-up-protocol.md` rewritten
   - New flow: SESSION-INDEX.md → archived-sessions.txt filter → user prompt → archive offer
   - Python sync script (`cowork_sync.py`) should generate the same SESSION-INDEX.md format

2. `9f7a646` — docs: add archive-state-is-server-side finding + updated data flow
   - Key finding: Claude Desktop stores archive flags server-side (Anthropic servers)
   - Local IndexedDB only has TipTap editor drafts — no conversation metadata
   - `archived-sessions.txt` is therefore the local source of truth for archive state

## Action items for python-macos maintainer

- [ ] Merge or rebase onto main to pick up both commits
- [ ] Verify `cowork_sync.py` generates SESSION-INDEX.md compatible with the catchup-bunny shortcut
- [ ] Ensure `archived-sessions.txt` is documented in the Python path's README/config examples
- [ ] Consider adding `config.example.json` key for `archived_sessions_file` path

## Context

The catchup-bunny mechanism now depends on:
- `SESSION-INDEX.md` (generated by sync script — both PS1 and Python versions must produce same format)
- `archived-sessions.txt` (manually maintained via catchup-bunny post-catchup offer)

The v1.0.0 release was tagged on main. python-macos should align before any v1.1.0 work.
